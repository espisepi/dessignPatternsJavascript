/*Tal cual su nombre lo dice este patrón se conforma
con la creación de un objeto que prácticamente es un JSON.*/

let myObjectLiteral = {

    suma : (a,b) => {return a+b;},

    resta : (a,b) => {return a-b}

}

console.log(myObjectLiteral.resta(2,4));